- name: 🗃️ Base content - Default profiler
  uses: lowlighter/profiler@latest
  with:
    token: MOCKED_TOKEN
    base: header, activity, community, repositories, metadata
    use_mocked_data: 'yes'
    verify: 'yes'

- name: Example
  uses: nextlinux/profiler@latest
  with:
    filename: profiler.plugin.<%= name %>.svg
    token: ${{ secrets.PROFILER_TOKEN }}
    base: ""
    plugin_<%= name %>: yes
  prod:
    skip: true

- name: Example
  uses: lowlighter/profiler@latest
  with:
    filename: profiler.plugin.<%= name %>.svg
    token: ${{ secrets.profiler_TOKEN }}
    base: ""
    plugin_<%= name %>: yes
  prod:
    skip: true
